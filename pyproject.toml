[tool.ruff]
target-version = "py312"

[tool.ruff.lint]
# Start with correctness errors; avoid a repository-wide style rewrite.
select = ["E9", "F63", "F7", "F82"]

[tool.pytest.ini_options]
pythonpath = ["backend"]
testpaths = ["backend/tests/api", "backend/tests/unit"]
